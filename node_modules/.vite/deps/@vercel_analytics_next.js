"use client";
//#region \0rolldown/runtime.js
var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __commonJSMin = (cb, mod) => () => (mod || cb((mod = { exports: {} }).exports, mod), mod.exports);
var __copyProps = (to, from, except, desc) => {
	if (from && typeof from === "object" || typeof from === "function") for (var keys = __getOwnPropNames(from), i = 0, n = keys.length, key; i < n; i++) {
		key = keys[i];
		if (!__hasOwnProp.call(to, key) && key !== except) __defProp(to, key, {
			get: ((k) => from[k]).bind(null, key),
			enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable
		});
	}
	return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", {
	value: mod,
	enumerable: true
}) : target, mod));
//#endregion
//#region optional-peer-dep:__vite-optional-peer-dep:react:@vercel/analytics
var require_analytics$1 = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = {};
	throw new Error(`Could not resolve "react" imported by "@vercel/analytics". Is it installed?`);
}));
//#endregion
//#region optional-peer-dep:__vite-optional-peer-dep:next/navigation.js:@vercel/analytics
var require_analytics = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	module.exports = {};
	throw new Error(`Could not resolve "next/navigation.js" imported by "@vercel/analytics". Is it installed?`);
}));
//#endregion
//#region node_modules/@vercel/analytics/dist/next/index.mjs
var import_analytics = /* @__PURE__ */ __toESM(require_analytics$1(), 1);
var import_analytics$1 = require_analytics();
var initQueue = () => {
	if (window.va) return;
	window.va = function a(...params) {
		if (!window.vaq) window.vaq = [];
		window.vaq.push(params);
	};
};
var name = "@vercel/analytics";
var version = "2.0.1";
function isBrowser() {
	return typeof window !== "undefined";
}
function detectEnvironment() {
	try {
		const env = "development";
		if (env === "development" || env === "test") return "development";
	} catch {}
	return "production";
}
function setMode(mode = "auto") {
	if (mode === "auto") {
		window.vam = detectEnvironment();
		return;
	}
	window.vam = mode;
}
function getMode() {
	return (isBrowser() ? window.vam : detectEnvironment()) || "production";
}
function isDevelopment() {
	return getMode() === "development";
}
function computeRoute(pathname, pathParams) {
	if (!pathname || !pathParams) return pathname;
	let result = pathname;
	try {
		const entries = Object.entries(pathParams);
		for (const [key, value] of entries) if (!Array.isArray(value)) {
			const matcher = turnValueToRegExp(value);
			if (matcher.test(result)) result = result.replace(matcher, `/[${key}]`);
		}
		for (const [key, value] of entries) if (Array.isArray(value)) {
			const matcher = turnValueToRegExp(value.join("/"));
			if (matcher.test(result)) result = result.replace(matcher, `/[...${key}]`);
		}
		return result;
	} catch {
		return pathname;
	}
}
function turnValueToRegExp(value) {
	return new RegExp(`/${escapeRegExp(value)}(?=[/?#]|$)`);
}
function escapeRegExp(string) {
	return string.replace(/[.*+?^${}()|[\]\\]/g, "\\$&");
}
function getScriptSrc(props) {
	if (props.scriptSrc) return makeAbsolute(props.scriptSrc);
	if (isDevelopment()) return "https://va.vercel-scripts.com/v1/script.debug.js";
	if (props.basePath) return makeAbsolute(`${props.basePath}/insights/script.js`);
	return "/_vercel/insights/script.js";
}
function loadProps(explicitProps, confString) {
	var _a;
	let props = explicitProps;
	if (confString) try {
		props = {
			...(_a = JSON.parse(confString)) == null ? void 0 : _a.analytics,
			...explicitProps
		};
	} catch {}
	setMode(props.mode);
	const dataset = {
		sdkn: name + (props.framework ? `/${props.framework}` : ""),
		sdkv: version
	};
	if (props.disableAutoTrack) dataset.disableAutoTrack = "1";
	if (props.viewEndpoint) dataset.viewEndpoint = makeAbsolute(props.viewEndpoint);
	if (props.eventEndpoint) dataset.eventEndpoint = makeAbsolute(props.eventEndpoint);
	if (props.sessionEndpoint) dataset.sessionEndpoint = makeAbsolute(props.sessionEndpoint);
	if (isDevelopment() && props.debug === false) dataset.debug = "false";
	if (props.dsn) dataset.dsn = props.dsn;
	if (props.endpoint) dataset.endpoint = props.endpoint;
	else if (props.basePath) dataset.endpoint = makeAbsolute(`${props.basePath}/insights`);
	return {
		beforeSend: props.beforeSend,
		src: getScriptSrc(props),
		dataset
	};
}
function makeAbsolute(url) {
	return url.startsWith("http://") || url.startsWith("https://") || url.startsWith("/") ? url : `/${url}`;
}
function inject(props = { debug: true }, confString) {
	var _a;
	if (!isBrowser()) return;
	const { beforeSend, src, dataset } = loadProps(props, confString);
	initQueue();
	if (beforeSend) (_a = window.va) == null || _a.call(window, "beforeSend", beforeSend);
	if (document.head.querySelector(`script[src*="${src}"]`)) return;
	const script = document.createElement("script");
	script.src = src;
	for (const [key, value] of Object.entries(dataset)) script.dataset[key] = value;
	script.defer = true;
	script.onerror = () => {
		const errorMessage = isDevelopment() ? "Please check if any ad blockers are enabled and try again." : "Be sure to enable Web Analytics for your project and deploy again. See https://vercel.com/docs/analytics/quickstart for more information.";
		console.log(`[Vercel Web Analytics] Failed to load script from ${src}. ${errorMessage}`);
	};
	document.head.appendChild(script);
}
function pageview({ route, path }) {
	var _a;
	(_a = window.va) == null || _a.call(window, "pageview", {
		route,
		path
	});
}
function getBasePath() {
	if (typeof process === "undefined" || typeof process.env === "undefined") return;
	return process.env.REACT_APP_VERCEL_OBSERVABILITY_BASEPATH;
}
function getConfigString() {
	if (typeof process === "undefined" || typeof process.env === "undefined") return;
	return process.env.REACT_APP_VERCEL_OBSERVABILITY_CLIENT_CONFIG;
}
function Analytics(props) {
	(0, import_analytics.useEffect)(() => {
		var _a;
		if (props.beforeSend) (_a = window.va) == null || _a.call(window, "beforeSend", props.beforeSend);
	}, [props.beforeSend]);
	(0, import_analytics.useEffect)(() => {
		inject({
			framework: props.framework || "react",
			basePath: props.basePath ?? getBasePath(),
			...props.route !== void 0 && { disableAutoTrack: true },
			...props
		}, props.configString ?? getConfigString());
	}, []);
	(0, import_analytics.useEffect)(() => {
		if (props.route && props.path) pageview({
			route: props.route,
			path: props.path
		});
	}, [props.route, props.path]);
	return null;
}
var useRoute = () => {
	const params = (0, import_analytics$1.useParams)();
	const searchParams = (0, import_analytics$1.useSearchParams)();
	const path = (0, import_analytics$1.usePathname)();
	if (!params) return {
		route: null,
		path
	};
	return {
		route: computeRoute(path, Object.keys(params).length ? params : Object.fromEntries(searchParams.entries())),
		path
	};
};
function getBasePath2() {
	if (typeof process === "undefined" || typeof process.env === "undefined") return;
	return process.env.NEXT_PUBLIC_VERCEL_OBSERVABILITY_BASEPATH;
}
function getConfigString2() {
	if (typeof process === "undefined" || typeof process.env === "undefined") return;
	return process.env.NEXT_PUBLIC_VERCEL_OBSERVABILITY_CLIENT_CONFIG;
}
function AnalyticsComponent(props) {
	const { route, path } = useRoute();
	return /* @__PURE__ */ import_analytics.default.createElement(Analytics, {
		path,
		route,
		...props,
		basePath: getBasePath2(),
		configString: getConfigString2(),
		framework: "next"
	});
}
function Analytics2(props) {
	return /* @__PURE__ */ import_analytics.default.createElement(import_analytics.Suspense, { fallback: null }, /* @__PURE__ */ import_analytics.default.createElement(AnalyticsComponent, { ...props }));
}
//#endregion
export { Analytics2 as Analytics };

//# sourceMappingURL=@vercel_analytics_next.js.map